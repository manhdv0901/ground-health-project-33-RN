import { LOADING } from "../actions/types";

const initialState ={
    isLoad: false,
}

export default function reducerLoading (state = initialState, action){
    switch(action.type){
        
    }
}